{
  "name": "digiclin-system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --names \"client,server\" -c \"bgGreen.bold,bgBlue.bold\" \"pnpm --filter client dev\" \"pnpm --filter server dev\"",
    "start": "pnpm --filter server start",
    "client:dev": "pnpm --filter client dev",
    "server:dev": "pnpm --filter server dev",
    "server:prod": "pnpm --filter server prod",
    "build:client": "rm -rf app/backend/dist && pnpm run --filter client build && cp -r app/frontend/dist app/backend/dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.15.1",
  "devDependencies": {
    "astro": "^5.13.7",
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "ky": "^1.10.0",
    "nanostores": "^1.0.1"
  }
}
