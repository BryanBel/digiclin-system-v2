
<slot />

<script>
  import AuthModule from "./authModule.js";
  (async () => {
    try {
      await AuthModule.getLoggedUser();
      window.location.replace('/');
    } catch (error) {
      // If it fails (e.g., 401 error), the user is not logged in. Do nothing.
    }
  })();
</script>