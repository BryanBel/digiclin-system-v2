---
import Layout from '../layouts/Layout.astro';
const locals = Astro.locals as { user?: unknown };
const user = locals.user ?? null;
const isLoggedIn = Boolean(user);
---
<Layout title="DigiClin | Gestiona tu salud">
  <section class="hero">
    <div class="hero__content">
      <div class="hero__text">
        <h1>Bienvenido a <span>DigiClin</span></h1>
        <p>Gestiona tus citas médicas y tu historial de manera simple y segura.</p>
        <div class="hero__actions">
          <a class="hero__btn hero__btn--primary" href="/schedule-appointments">
            <svg aria-hidden="true" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="4" y="5" width="16" height="16" rx="2" />
              <path d="M16 3v4" />
              <path d="M8 3v4" />
              <path d="M4 11h16" />
            </svg>
            Agendar cita
          </a>
          {!isLoggedIn && (
            <a class="hero__btn hero__btn--secondary" href="/login">
              <svg aria-hidden="true" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M15 8v-2a2 2 0 0 0 -2 -2h-7a2 2 0 0 0 -2 2v12a2 2 0 0 0 2 2h7a2 2 0 0 0 2 -2v-2" />
                <path d="M21 12h-11" />
                <path d="M17 16l4 -4l-4 -4" />
              </svg>
              Ingresar
            </a>
          )}
        </div>
      </div>

      <div class="hero__card">
        <div class="hero__icon">
          <svg aria-hidden="true" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 3a12 12 0 0 0 8.5 3a12 12 0 0 1 -8.5 15a12 12 0 0 1 -8.5 -15a12 12 0 0 0 8.5 -3" />
          </svg>
        </div>
        <h2>Tu salud, organizada</h2>
        <p>Accede a tus citas, historial y recetas en un solo lugar.</p>
      </div>
    </div>
  </section>
</Layout>